/**
 * Reference middlewares and demo agents for the
 * deepagents-acp server. The classes in this package are
 * marked {@code @Example} in their Javadoc to flag them as
 * illustrative; they are not on the core hot path and
 * should not be used as production middleware.
 */
package org.aethercode.acp.examples;
