/**
 * Remote client and non-interactive (CI) entry points.
 *
 * <p>Java 21 port of the Python
 * {@code deepagents_code.client} package. The non-interactive
 * entry point drives the agent loop in headless mode; the remote
 * client speaks the agent HTTP API.</p>
 */
package org.aethercode.code.client;
