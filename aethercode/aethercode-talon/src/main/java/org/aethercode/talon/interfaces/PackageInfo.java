/**
 * Protocol interfaces for Talon host integrations.
 *
 * <p>Java-native port of the Python
 * {@code deepagents_talon.interfaces} module. Defines the data records
 * (channel message / reaction / status, agent request / result) and
 * the protocol interfaces (channel adapter, agent runtime, cron
 * scheduler) the host and adapters program against.</p>
 */
package org.aethercode.talon.interfaces;
