package org.aethercode.code.tui.widgets;

/**
 * Stub for the Codex auth screen file. The actual screen implementation
 * lives in {@link CodexAuth.AuthScreen} and {@link CodexAuth.SignedInScreen};
 * this class exists so the file name {@code CodexAuthScreen.java} matches
 * its public class and Java's "public type in matching file" rule is
 * satisfied.
 *
 * <p>The auth screens are referenceable under their original Python
 * module name; downstream code uses {@code CodexAuthScreen} as a facade
 * over {@link CodexAuth}.</p>
 */
public final class CodexAuthScreen {
    private CodexAuthScreen() {}
}
